int multiplyNumbers(int m, int n) {
   
if(m==0 || n==0)
    return 0;
    int ans=multiplyNumbers(m-1,n);
    return n+ans;
}
#include <iostream>
#include "solution.h"
using namespace std;

int main() {
    int m, n;
    cin >> m >> n;
    cout << multiplyNumbers(m, n) << endl;
}
